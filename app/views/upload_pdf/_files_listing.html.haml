- Dir.glob(Rails.root.to_s + "'/public/uploads/pdf'/#{type}/*.pdf").each do |file|
  %ul
    %li
      = link_to File.basename(file).humanize,download_crawlers_path(type: type.to_sym,filename: File.basename(file))